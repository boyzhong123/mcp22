import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Speech Assessment Runtime & Operations | Chivox AI',
  description: 'Operate a production speech assessment API with scoped keys, enforceable usage limits, proactive alerts, live analytics, zero-retention streaming, and enterprise SLA controls.',
  alternates: { canonical: '/runtime' },
};

export { default } from '../global/runtime/page';
